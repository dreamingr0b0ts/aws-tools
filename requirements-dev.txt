# Dev/test dependencies (the tools themselves only need boto3).
#
# Runtime deps use a bounded major range so the tools keep receiving bug/security
# patches without absorbing breaking changes. Dev tooling is pinned more tightly
# for reproducible CI -- in particular ruff, whose formatter output can change
# between minor releases and now gates CI via `ruff format --check`.
boto3>=1.34,<2
moto[ec2,s3]>=5,<6
pytest>=8,<10
ruff>=0.15,<0.16
